# Fix List - AI Terminal IDE (2025-03-01)

## 優先度：高

### 1. .pkgファイルクリック時のフリーズ対策
- **現状**: .pkgファイルをクリックするとアプリがフリーズする
- **原因**: PDF webviewで.pkgファイルを開こうとしている
- **修正内容**: 
  - .pkgファイルをクリックした場合は、システムのデフォルトアプリで開く
  - または「このファイル形式はサポートされていません」を表示
- **変更箇所**: `src/renderer/index.ts` - `handleFileClick()` 関数

### 2. WebサーバーURLのクリックでコピー
- **現状**: サーバーIPが表示されるだけ
- **修正内容**:
  - `#server-ip`をクリックするとURLがクリップボードにコピーされる
  - コピー後に「Copied!」などのフィードバックを表示
- **変更箇所**: 
  - `src/renderer/index.html` - `#server-status` にクリック属性追加
  - `src/renderer/index.ts` - クリックハンドラ追加
  - `src/renderer/index.css` - ホバー時のスタイル追加

---

## 優先度：中

### 3. ターミナル修正（isShuttingDown）
- **現状**: PTY作成時に `isShuttingDown is not defined` エラー
- **修正内容**: ✅ 完了
- **変更箇所**: `src/main/skillsManager.ts` - 変数宣言を追加

---

## 優先度：中（将来的なタスク）

### 4. スマホ・タブレット対応レスポンシブビュー

#### 4.1 スマホビュー (幅 < 768px)
- ワークスペースバー: アイコンのみ表示（名前は非表示）
- ターミナル: 全幅表示
- Explorer: 下部にスワイプで表示
- アクティビティバー: 非表示またはボトムバー化

#### 4.2 タブレットビュー (768px <= 幅 < 1024px)
- ワークスペースバー: 縮小版
- ターミナルとExplorer: サイドバイサイド
- アクティビティバー: コンパクト版

#### 実装内容:
- CSS Media Queriesの追加
- タッチ操作対応（スワイプでパネル切り替え）
- モバイル用のナビゲーション
- ファイルツリーのタッチスクロール改善

#### 変更箇所:
- `src/renderer/index.css` - レスポンシブスタイル追加
- `src/renderer/index.ts` - タッチイベント処理追加
- `src/renderer/index.html` - モバイル用UI要素追加（オプション）

---

## 実装順序

1. **.pkgファイル対策** - ユーザビリティ向上
2. **URLクリックコピー** - UX向上
3. **スマホ・タブレット対応** - 将来的なタスク（時間があれば）

---

## テスト計画

### Test 1: .pkgファイル対策
- .pkgファイルをクリック → エラーメッセージまたは外部アプリで開く
- 通常のPDFファイルをクリック → PDFビューワーで開く

### Test 2: URLクリックコピー
- サーバーIPをクリック → クリップボードにコピー
- フィードバック表示確認

### Test 3: ターミナル動作確認
- ワークスペース切り替え → ターミナルが正常に動作
- コマンド入力可能
